import logo from "../../img/logo.png";

const Header = () => {
  return (
    <header className="center">
      <img src={logo} alt="Breaking bad App" />
    </header>
  );
};

export default Header;
